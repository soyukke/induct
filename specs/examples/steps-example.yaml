name: steps example
description: |
  複数のコマンドを順番に実行し、それぞれの出力を検証する。
  ステップは順次実行され、失敗時は残りがスキップされる。

steps:
  - name: step one
    command: echo "hello"
    expect_output: "hello\n"

  - name: step two
    command: echo "world"
    expect_output: "world\n"
