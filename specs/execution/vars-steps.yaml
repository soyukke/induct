name: vars expands in steps
description: |
  vars は steps の各コマンドに��適用される。
test:
  command: ./zig-out/bin/induct run specs/fixtures/vars-steps.yaml
  expect_output_contains: "[PASS]"
  expect_output_not_contains: "[FAIL]"
  expect_exit_code: 0
